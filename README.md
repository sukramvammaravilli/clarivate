**Prerequisites needed** :-
Node.js : (version 16 or 16 +)
MySQL Server (version 8)
MySQL Workbench (version 8)

**Setup**:-
take pull from my github link.
type npm install
type "node index.js"
mysql setup 

**Configuration**:-
mysql configuration need to be configured in .env also

